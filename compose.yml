version: '3'

services:
  db:
    image: postgres:15
    container_name: $PROJECT-db
    environment:
      POSTGRES_PASSWORD: $PGPASSWORD
    volumes:
      - psql_data:/var/lib/postgresql/data
    ports:
      - 127.0.0.1:5432:5432

volumes:
  psql_data:
